# 🔗 Руководство по настройке PlanFix CRM

## 📋 Описание

Интеграция PlanFix CRM позволяет автоматически создавать задачи и контакты в вашей CRM системе при отправке форм с сайта.

---

## 🔧 Настройка PlanFix API

### Шаг 1: Получение API ключей

1. Войдите в ваш аккаунт PlanFix
2. Перейдите в **Настройки** → **API**
3. Создайте новый API ключ:
   - **API Key** - публичный ключ
   - **API Secret** - секретный ключ
4. Скопируйте **Account Name** (название вашего аккаунта)

### Шаг 2: Настройка файла `planfix.php`

Откройте файл `planfix.php` и замените следующие значения:

```php
$planfix_api_key = "YOUR_API_KEY";        // Ваш API Key
$planfix_api_secret = "YOUR_API_SECRET";  // Ваш API Secret
$planfix_account = "YOUR_ACCOUNT_NAME";   // Название аккаунта
```

**Пример:**
```php
$planfix_api_key = "abc123xyz";
$planfix_api_secret = "secret_key_here";
$planfix_account = "mycompany";
```

---

## 📤 Что отправляется в PlanFix

### Обычная форма обратной связи:
- **Тип:** Задача
- **Название:** "Новая заявка с сайта: [Услуга]"
- **Описание:** Имя, телефон, email, услуга, сообщение
- **Статус:** Новая
- **Приоритет:** Обычный

### Форма обратного звонка:
- **Тип:** Задача
- **Название:** "Запрос на обратный звонок"
- **Описание:** Телефон клиента

### Форма для госзаказчиков:
- **Тип:** Задача
- **Название:** "Запрос КП для госзакупки: [Организация]"
- **Описание:** Все данные из формы (сроки, требования, контакты)

---

## 🔄 Как это работает

1. Пользователь заполняет форму на сайте
2. Данные отправляются в `telegram.php`
3. Данные проверяются (валидация, защита от спама)
4. Отправка в Telegram (как раньше)
5. **НОВОЕ:** Отправка в PlanFix CRM
6. Создается задача в PlanFix
7. Пользователь перенаправляется на страницу благодарности

---

## ⚙️ Дополнительные настройки

### Изменение статуса задачи

В файле `planfix.php` найдите функцию `createPlanFixTask()` и измените:

```php
$task->status = 1; // 1 = Новая, 2 = В работе, и т.д.
$task->priority = 2; // 1 = Низкий, 2 = Обычный, 3 = Высокий
```

### Создание контакта

По умолчанию создается только задача. Чтобы также создавать контакт, раскомментируйте в `telegram.php`:

```php
// После отправки задачи
if (function_exists('createPlanFixContact')) {
    createPlanFixContact($token, $_POST);
}
```

---

## 🧪 Тестирование

### Проверка работы интеграции:

1. Заполните тестовую форму на сайте
2. Проверьте, что заявка появилась в Telegram
3. Войдите в PlanFix и проверьте раздел "Задачи"
4. Должна появиться новая задача с данными из формы

### Если не работает:

1. Проверьте правильность API ключей в `planfix.php`
2. Проверьте логи ошибок PHP (обычно в `error_log`)
3. Убедитесь, что API включен в настройках PlanFix
4. Проверьте права доступа API ключа

---

## 📝 Логирование ошибок

Ошибки PlanFix API записываются в лог PHP. Для просмотра:

```php
// В planfix.php добавьте отладочный вывод:
error_log("PlanFix Debug: " . print_r($data, true));
```

Или включите отображение ошибок в PHP (только для разработки):

```php
ini_set('display_errors', 1);
error_reporting(E_ALL);
```

---

## 🔒 Безопасность

- ✅ API ключи хранятся в PHP файле (не в HTML)
- ✅ Все данные валидируются перед отправкой
- ✅ Используется HTTPS для API запросов
- ✅ Защита от спама работает до отправки в PlanFix

---

## 📚 Документация PlanFix API

Полная документация API доступна на:
- https://planfix.com/help/Planfix_API

Основные методы:
- `auth.login` - аутентификация
- `task.add` - создание задачи
- `contact.add` - создание контакта

---

## ✅ Чек-лист настройки

- [ ] Получены API ключи из PlanFix
- [ ] Обновлены настройки в `planfix.php`
- [ ] Протестирована отправка формы
- [ ] Проверено создание задачи в PlanFix
- [ ] Настроены статусы и приоритеты (опционально)
- [ ] Включено создание контактов (опционально)

---

## 💡 Рекомендации

1. **Тестирование:** Сначала протестируйте на тестовом аккаунте PlanFix
2. **Мониторинг:** Регулярно проверяйте создание задач в PlanFix
3. **Настройка:** Настройте автоматические уведомления в PlanFix для новых задач
4. **Категории:** Используйте разные статусы для разных типов заявок

---

**Статус:** ✅ Интеграция готова к использованию  
**Требуется:** Настройка API ключей в `planfix.php`
