# Быстрый старт для разработчиков

## 🚀 Быстрая настройка проекта

### 1. Клонирование и запуск

```bash
# Перейти в директорию проекта
cd /path/to/project

# Запустить локальный сервер
python -m http.server 8000

# Или с Node.js
npx http-server -p 8000
```

Откройте в браузере: http://localhost:8000/index.html

---

## 📁 Основные файлы

| Файл | Назначение |
|------|-----------|
| `index.html` | Главная страница сайта |
| `calculator.html` | Страница калькулятора стоимости |
| `gov-customers.html` | Раздел для госзаказчиков |
| `styles.css` | Все стили сайта |
| `script.js` | Основная логика (навигация, формы, карта) |
| `calculator.js` | Логика калькулятора |
| `gov-customers.js` | Логика страницы госзаказчиков |

---

## 🔧 Быстрые изменения

### Изменить цвет сайта

**Файл:** `styles.css` (строки 7-22)

```css
:root {
    --primary-color: #2c5aa0;  /* Изменить здесь */
    --accent-color: #ff6b35;   /* И здесь */
}
```

### Добавить новую услугу в калькулятор

**Файл:** `calculator.js` (строки 15-50)

```javascript
const services = {
    // Добавить новую услугу:
    newService: {
        name: 'Новая услуга',
        price: 10000,
        unit: 'м²',
        defaultArea: 500
    }
};
```

**Файл:** `calculator.html` (строки 60-100)

```html
<button type="button" class="option-btn service-type-btn" 
        data-service="newService">
    <span class="option-label">Новая услуга</span>
    <span class="option-price">от 10 000 руб/м²</span>
</button>
```

### Изменить контактные данные

**Файлы:** `index.html`, `calculator.html`, `gov-customers.html`

Найти и заменить:
- Телефон: `+7 (921) 625–34–41`
- Email: `info@amnis.ru`
- Адрес: `196084, г. Санкт-Петербург...`

---

## 🐛 Отладка

### Проблемы с калькулятором

1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Проверьте, что `calculator.js` загружен
4. Убедитесь, что все кнопки имеют правильные классы

### Проблемы с формами

1. Проверьте настройку Telegram в `script.js`
2. Проверьте консоль на ошибки отправки
3. Убедитесь, что токен и chat_id указаны

### Проблемы с картой

1. Проверьте наличие Yandex Maps API ключа
2. Проверьте загрузку `assets/data/projects.json`
3. Проверьте консоль на ошибки API

---

## 📝 Частые задачи

### Добавить новый проект на карту

**Файл:** `assets/data/projects.json`

Добавить объект в массив `projects`:
```json
{
    "id": 100,
    "title": "Новый проект",
    "location": "Москва",
    "coordinates": [55.7558, 37.6176],
    "year": 2024,
    "type": "facade",
    "region": "moscow",
    "area": "5 000 м²",
    "date": "Январь 2024",
    "challenge": "Задача",
    "solution": "Решение",
    "results": ["Результат"],
    "clientLogo": "",
    "photos": [],
    "caseLink": "#"
}
```

### Изменить текст на главной странице

**Файл:** `index.html`

Используйте поиск (Ctrl+F) для нахождения нужного текста.

### Добавить новую секцию

1. Добавить HTML в нужный файл
2. Добавить стили в `styles.css`
3. При необходимости добавить JavaScript

---

## 🔗 Полезные ссылки

- **Полная документация:** `DOCUMENTATION.md`
- **Настройка Telegram:** `TELEGRAM_SETUP.md`
- **Добавление проектов:** `PROJECTS_GUIDE.md`

---

## ⚠️ Важные замечания

1. **Не удаляйте** файлы `calculator.js` и `gov-customers.js` - они используются
2. **Не изменяйте** структуру данных в `calculator.js` без обновления HTML
3. **Всегда тестируйте** изменения в разных браузерах
4. **Проверяйте** мобильную версию после изменений

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Проверьте документацию `DOCUMENTATION.md`
3. Убедитесь, что все файлы на месте
4. Проверьте пути к файлам (относительные пути)


