# 📊 Руководство по настройке аналитики

## 📋 Текущее состояние

**Статус:** Код аналитики добавлен, но не активирован (закомментирован)

**Цель:** Настроить Яндекс.Метрику и Google Analytics для отслеживания эффективности сайта.

---

## 🎯 Яндекс.Метрика

### Шаг 1: Создание счетчика

1. Перейти на https://metrika.yandex.ru/
2. Войти в аккаунт Яндекс
3. Нажать "Добавить счетчик"
4. Заполнить данные:
   - **Название:** ООО «АМНИС» - сайт
   - **Адрес сайта:** https://amnis-geo.ru
   - **Часовой пояс:** Москва (UTC+3)
   - **Валюта:** Российский рубль (RUB)

### Шаг 2: Получение ID счетчика

После создания счетчика вы получите **номер счетчика** (например: 12345678)

### Шаг 3: Активация кода

**Файл:** `index.html` (строки ~1678-1685)

**Найти:**
```javascript
// Замените XXXXXXXX на ваш номер счетчика Яндекс.Метрики
// ym(XXXXXXXX, "init", {clickmap:true,trackLinks:true,accurateTrackBounce:true});
```

**Заменить на:**
```javascript
ym(12345678, "init", {clickmap:true,trackLinks:true,accurateTrackBounce:true});
```

Где `12345678` - ваш номер счетчика.

**Также обновить noscript:**
```html
<noscript><div><img src="https://mc.yandex.ru/watch/12345678" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
```

### Шаг 4: Настройка целей

В Яндекс.Метрике настроить следующие цели:

#### Цель 1: Отправка формы обратной связи
- **Тип:** JavaScript-событие
- **Условие:** `form_submit` и `form_type = request`

#### Цель 2: Отправка формы обратного звонка
- **Тип:** JavaScript-событие
- **Условие:** `form_submit` и `form_type = callback`

#### Цель 3: Переход в калькулятор
- **Тип:** Переход по URL
- **Условие:** URL содержит `calculator.html`

#### Цель 4: Расчет стоимости в калькуляторе
- **Тип:** JavaScript-событие
- **Условие:** `calculator_result` или `calculation_complete`

#### Цель 5: Отправка формы калькулятора
- **Тип:** JavaScript-событие
- **Условие:** `form_submit` и `form_type = calculator`

### Шаг 5: Добавление отслеживания событий

**Файл:** `script.js` или `telegram.php`

Добавить отправку событий в Яндекс.Метрику:

```javascript
// После успешной отправки формы
if (typeof ym !== 'undefined') {
    ym(12345678, 'reachGoal', 'form_submit', {
        form_type: 'request' // или 'callback', 'calculator'
    });
}

// После расчета стоимости в калькуляторе
if (typeof ym !== 'undefined') {
    ym(12345678, 'reachGoal', 'calculator_result', {
        service: calculatorData.serviceType,
        cost: finalCost
    });
}
```

---

## 📈 Google Analytics 4

### Шаг 1: Создание аккаунта

1. Перейти на https://analytics.google.com/
2. Войти в аккаунт Google
3. Нажать "Начать измерение"
4. Создать аккаунт аналитики:
   - **Название аккаунта:** ООО «АМНИС»
   - **Название ресурса:** amnis-geo.ru
   - **URL:** https://amnis-geo.ru
   - **Отрасль:** Строительство / Профессиональные услуги
   - **Часовой пояс:** Москва (UTC+3)

### Шаг 2: Получение Measurement ID

После создания ресурса вы получите **Measurement ID** (формат: `G-XXXXXXXXXX`)

### Шаг 3: Активация кода

**Файл:** `index.html` (строки ~1689-1696)

**Найти:**
```javascript
// Замените G-XXXXXXXXXX на ваш ID Google Analytics
// window.dataLayer = window.dataLayer || [];
// function gtag(){dataLayer.push(arguments);}
// gtag('js', new Date());
// gtag('config', 'G-XXXXXXXXXX');
```

**Заменить на:**
```javascript
window.dataLayer = window.dataLayer || [];
function gtag(){dataLayer.push(arguments);}
gtag('js', new Date());
gtag('config', 'G-XXXXXXXXXX');
```

Где `G-XXXXXXXXXX` - ваш Measurement ID.

**Также обновить скрипт загрузки:**
```html
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
```

### Шаг 4: Настройка событий

В Google Analytics настроить следующие события:

#### Событие: form_submit
```javascript
gtag('event', 'form_submit', {
    'form_type': 'request', // или 'callback', 'calculator'
    'form_name': 'Обратная связь'
});
```

#### Событие: calculator_used
```javascript
gtag('event', 'calculator_used', {
    'service_type': 'facade',
    'estimated_cost': 50000
});
```

#### Событие: phone_click
```javascript
gtag('event', 'phone_click', {
    'contact_method': 'phone'
});
```

---

## 🔧 Интеграция с формами

### Обновление script.js

Добавить отправку событий после успешной отправки форм:

```javascript
// В функции отправки формы обратной связи
requestForm.addEventListener('submit', async (e) => {
    // ... существующий код ...
    
    // Отправка события в Яндекс.Метрику
    if (typeof ym !== 'undefined') {
        ym(12345678, 'reachGoal', 'form_submit', {
            form_type: 'request'
        });
    }
    
    // Отправка события в Google Analytics
    if (typeof gtag !== 'undefined') {
        gtag('event', 'form_submit', {
            'form_type': 'request',
            'form_name': 'Обратная связь'
        });
    }
});
```

### Обновление calculator.js

Добавить отслеживание использования калькулятора:

```javascript
// После расчета стоимости
function calculateResult() {
    // ... существующий код расчета ...
    
    // Отправка события в Яндекс.Метрику
    if (typeof ym !== 'undefined') {
        ym(12345678, 'reachGoal', 'calculator_result', {
            service: calc.service,
            cost: finalCost
        });
    }
    
    // Отправка события в Google Analytics
    if (typeof gtag !== 'undefined') {
        gtag('event', 'calculator_used', {
            'service_type': calc.service,
            'estimated_cost': finalCost
        });
    }
}
```

---

## 📊 Настройка конверсий

### Яндекс.Метрика

1. Перейти в раздел "Настройки" → "Цели"
2. Создать следующие цели:

| Название цели | Тип | Условие |
|--------------|-----|---------|
| Отправка формы | JavaScript-событие | `form_submit` |
| Переход в калькулятор | Переход по URL | URL содержит `calculator.html` |
| Расчет стоимости | JavaScript-событие | `calculator_result` |
| Клик по телефону | JavaScript-событие | `phone_click` |

### Google Analytics 4

1. Перейти в раздел "Администратор" → "События"
2. Создать следующие события:

| Название события | Параметры |
|-----------------|-----------|
| form_submit | form_type, form_name |
| calculator_used | service_type, estimated_cost |
| phone_click | contact_method |

---

## ✅ Чек-лист настройки

- [ ] Создан счетчик Яндекс.Метрики
- [ ] Получен номер счетчика
- [ ] Активирован код Яндекс.Метрики в `index.html`
- [ ] Обновлен noscript тег
- [ ] Настроены цели в Яндекс.Метрике
- [ ] Создан аккаунт Google Analytics 4
- [ ] Получен Measurement ID
- [ ] Активирован код Google Analytics в `index.html`
- [ ] Добавлены события в `script.js`
- [ ] Добавлены события в `calculator.js`
- [ ] Протестирована отправка событий
- [ ] Проверена работа аналитики в реальном времени

---

## 🧪 Тестирование

### Проверка Яндекс.Метрики

1. Открыть сайт в браузере
2. Выполнить действия (отправить форму, использовать калькулятор)
3. Перейти в Яндекс.Метрику → "Отчеты" → "Онлайн"
4. Проверить, что события фиксируются

### Проверка Google Analytics

1. Открыть сайт в браузере
2. Выполнить действия
3. Перейти в Google Analytics → "Отчеты" → "В реальном времени"
4. Проверить, что события фиксируются

### Инструменты для отладки

1. **Chrome DevTools** → Network tab
   - Проверить запросы к `mc.yandex.ru` и `google-analytics.com`

2. **Расширение для Chrome:**
   - Яндекс.Метрика: "Яндекс.Метрика" (официальное расширение)
   - Google Analytics: "Google Analytics Debugger"

---

## 📝 Важные замечания

1. **Не удаляйте закомментированный код** - он может понадобиться для отладки
2. **Используйте один и тот же ID** во всех местах кода
3. **Тестируйте на реальных данных** перед запуском в продакшн
4. **Соблюдайте GDPR/152-ФЗ** - уведомляйте пользователей об использовании аналитики
5. **Проверяйте работу регулярно** - аналитика может перестать работать после обновлений

---

## 🔒 Конфиденциальность

### Соответствие 152-ФЗ

Убедитесь, что:
- ✅ Пользователи уведомлены об использовании аналитики (cookie banner)
- ✅ Есть возможность отказаться от аналитики
- ✅ Данные не передаются третьим лицам без согласия

### Настройка анонимизации IP

**Яндекс.Метрика:**
```javascript
ym(12345678, "init", {
    clickmap: true,
    trackLinks: true,
    accurateTrackBounce: true,
    webvisor: true,
    params: {
        anonymize_ip: true // Анонимизация IP
    }
});
```

**Google Analytics:**
```javascript
gtag('config', 'G-XXXXXXXXXX', {
    'anonymize_ip': true // Анонимизация IP
});
```

---

## 📞 Поддержка

Если возникнут проблемы:

1. **Яндекс.Метрика:**
   - Документация: https://yandex.ru/support/metrica/
   - Форум: https://yandex.ru/support/metrica/forum.html

2. **Google Analytics:**
   - Документация: https://developers.google.com/analytics
   - Справочный центр: https://support.google.com/analytics

---

**Статус:** Готово к настройке  
**Время настройки:** 30-60 минут
